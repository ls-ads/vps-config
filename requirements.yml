---
# Ansible Galaxy collections. Install with:
#   ansible-galaxy collection install -r requirements.yml -p ./.galaxy

collections:
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.docker
    version: ">=3.4.0"
  - name: community.crypto
    version: ">=2.16.0"
