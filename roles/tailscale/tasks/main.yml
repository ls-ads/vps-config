---
# tailscale role — install, authenticate, expose tailscale0 IP as a
# host fact for nginx + ssl roles to use.

# --- Install --------------------------------------------------------------

- name: Check whether the tailscale binary is already installed
  command: which tailscale
  register: tailscale_bin
  changed_when: false
  failed_when: false

- name: Install Tailscale apt signing key
  get_url:
    url: "https://pkgs.tailscale.com/stable/ubuntu/{{ ansible_distribution_release }}.noarmor.gpg"
    dest: /usr/share/keyrings/tailscale-archive-keyring.gpg
    mode: "0644"
  when: tailscale_bin.rc != 0

- name: Install Tailscale apt source list
  get_url:
    url: "https://pkgs.tailscale.com/stable/ubuntu/{{ ansible_distribution_release }}.tailscale-keyring.list"
    dest: /etc/apt/sources.list.d/tailscale.list
    mode: "0644"
  when: tailscale_bin.rc != 0

- name: Install Tailscale package
  apt:
    name: tailscale
    state: present
    update_cache: true

- name: Ensure tailscaled is enabled and running
  service:
    name: tailscaled
    state: started
    enabled: true

# --- Authenticate ---------------------------------------------------------

- name: Query current Tailscale backend state
  command: tailscale status --json
  register: tailscale_state
  changed_when: false
  failed_when: false

- name: Authenticate to tailnet (first run / re-auth only)
  command: >
    tailscale up
      --authkey={{ tailscale_auth_key }}
      --hostname={{ tailscale_hostname }}
      --accept-dns={{ tailscale_accept_dns | ternary('true', 'false') }}
      --ssh={{ tailscale_ssh | ternary('true', 'false') }}
  when: >
    tailscale_state.rc != 0
    or (tailscale_state.stdout | from_json).BackendState != 'Running'
  no_log: true

# --- Expose tailscale0 IPv4 as a host fact --------------------------------

- name: Read tailscale0 IPv4 address
  command: tailscale ip -4
  register: tailscale_ip_lookup
  changed_when: false

- name: Set tailscale_ip fact (cacheable — nginx + ssl roles rely on it)
  set_fact:
    tailscale_ip: "{{ tailscale_ip_lookup.stdout_lines[0] }}"
    cacheable: true

# --- Status summary -------------------------------------------------------
# One-shot reminder of which DNS records the operator needs to maintain
# in Cloudflare pointing at this IP. Derived from apps + ops_tools +
# env_prefix so the list stays correct as config changes.

- name: Compute ops-tool DNS targets (one per tool in ops_tools)
  set_fact:
    _tailscale_ops_targets: >-
      [{% for tool in ops_tools %}'{{ tool.name }}.{{ ops_subdomain }}.{{ env_prefix }}{{ apex_domain }}'{% if not loop.last %},{% endif %}{% endfor %}]

- name: Compute app DNS targets (UAT only — apps are Tailscale-gated there)
  set_fact:
    _tailscale_app_targets: >-
      [{% for app in apps %}'{{ env_prefix }}{{ app.base_domain }}'{% if not loop.last %},{% endif %}{% endfor %}]
  when: env == 'uat'

- name: Combine into tailscale_dns_targets
  set_fact:
    tailscale_dns_targets: "{{ _tailscale_ops_targets + (_tailscale_app_targets | default([])) }}"

- name: Show DNS records that should resolve to this Tailscale IP
  debug:
    msg: |
      tailscale0 IP for {{ inventory_hostname }} ({{ env }}): {{ tailscale_ip }}

      Ensure the following Cloudflare A records resolve here (DNS-only,
      grey cloud — Tailscale IPs can't be proxied):

      {% for d in tailscale_dns_targets %}  - {{ d }}
      {% endfor %}
